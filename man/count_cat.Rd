% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary_statistics.R
\name{count_cat}
\alias{count_cat}
\title{Frequency of categorical variables}
\usage{
count_cat(x, width = 20, collapse = FALSE, sort = TRUE, format = TRUE)
}
\arguments{
\item{x}{Vector or data frame of categorical variables.}

\item{width}{Integer specifying the maximum width for wrapping text.}

\item{collapse}{Logical indicating whether to merge categories with identical proportions.}

\item{sort}{Logical or character vector. If \code{TRUE}, orders categories by frequency. If \code{FALSE}, orders by names. If a character vector, renames and orders categories accordingly.}

\item{format}{Logical indicating whether to format category names if the input is a vector.}
}
\value{
Data frame with two columns: \code{f} (category names) and \code{n} (frequency counts).
}
\description{
Formats a data frame or vector containing categorical variables and calculates the frequency of each category.
}
\examples{
# Vector of categorical variable
k <- 10
n <- runif(k, 1, 10) \%>\% round()
x <- paste("Level", seq(k)) \%>\%
    mapply(function(x, y) rep(x, y), ., n) \%>\%
    unlist()
count_cat(x)

# Data frame of categorical variable
df <- sapply(seq(10), function(x) runif(10) \%>\% round()) \%>\% as.data.frame()
colnames(df) <- paste("Level", seq(10))
count_cat(df)

}
